# Part 1 
- First created a class named "Student" and created "mem" dictionary to store the data of students.
- Then created function calculate_total(self, sub) to calculated Total marks and return it
- Then created function calculate_percentage(self, sub) to calculate percentage and return it for both function list of marks is given. 
- Then created function assign_grade(self, percent) to return the grade based on there percentage
- Then created function std_add(self, std_id, name, sub) to store student info in memory. In this all needed function is called for calculating Total marks, percentage and grads and then stored it.
- Then display_details(self, std) this function is used to display the students information
- Here infinite while loop is runned and when user chooses to exit it breaks that while loop

# Part 2
- Here infinite while loop is runned and when user chooses to exit it breaks that while loop
- In parent class all the information of employee is been added and stored in the "mem" dictionary
- In child class using super class add_emp function of parent class is called in child class and is been used to store employee information
- In child class information of certain employee based on there emp_id is displayed 

# Part 3
- In this task I have used employee management task
- In this using CSV library data is been written and stored in .CSV file
- First the header row and then employee information is been stored in this file