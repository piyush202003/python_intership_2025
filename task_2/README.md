# Part 1
- Took name of studens 
- Took marks of 5 stubjects and stored it in list named "sub"
- Created function named "total_calculation" and passing "sub" list as parameter and returning total sum of marks of subjects
- Created function named "percentage_calculation" and returns the obtained percentage
- Created function named "grade_assignment" and passing obtained percentage as parameter and using if-else for evaluation based of percentage

# Part 2
- Took password input as string 
- Created function named "validate" for validation and passing "password" as parameter which gives result
- Created string for uppercase, lowercase, digits and special
- Using for loop for the password string to traverse through every characeter in password string
- Then checking if that if character is present in the given strings of uppercase, lowercase, digits or special case if present then uppending there count
- Then if password have all the given cases then it will print valid
- Or else if any characeter is not present then corresponding to that its value will be zero and will print the reason

# Part 3
- First declaring the main dictionary and starting id
- Then using while loop for unlimited time
- Then if user choose 1 for adding employee name then it will have to give no. of employee user wants to enter and there info like name and salary
- Then "add_emp" function will create new dictionary for that employee inside the main dictionary and store its information with HRA, DA and Total salary calculaion
- Then if using Wants to print inserted information then it will print all the given information in the dictionary using for loop